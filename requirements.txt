# Core numerics and configuration
numpy>=1.26.0
pyyaml>=6.0.1
tqdm>=4.66.0
psutil>=5.9.8

# PyTorch is installed separately so the CUDA build matches your driver:
#   CUDA 12.6  pip install torch --index-url https://download.pytorch.org/whl/cu126
#   CPU only   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.4.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0

# Provider HTTP client. httpx is required, not optional: every API provider
# is reached over its streaming interface.
httpx>=0.27.0

# Tests
pytest>=8.0.0
